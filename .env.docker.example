# Copy to .env.docker (optional) or export before: docker compose --env-file .env.docker up
HTTP_PORT=8080
PUBLIC_APP_URL=http://localhost:8080
MAIL_DRIVER=log
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAIL_FROM=customercare@legacyenergy.ca
